/**
 * InputHandler (vazio) — a implementar na Fase 2.
 *
 * Responsabilidades previstas:
 *   - Centralizar toda a leitura de teclado (keydown / keyup)
 *   - Expor um estado simples { forward, backward, left, right, fire }
 *     que o Player consome em cada frame — desacopla input de lógica
 *   - Suporte a múltiplas teclas em simultâneo (movimento + rotação + disparo)
 *   - Possível extensão para gamepad (Gamepad API)
 */
